# simulate25b

```bash
go install github.com/seqyuan/simulate25b@latest
```


```bash
version="v0.1.1" && \
git add -A && git commit -m $version && git tag $version && git push origin main && git push origin $version
```
